# iOS 版与原版 Android 功能对齐核对

> 生成时间：2026-09-17
> 核对方式：代码检索（`--include=*.swift` 全量 grep）+ `git log` 批次落地确认 + `PRD-zhisheng-ios-A.md` §6 备案比对
> 结论口径：**「缺失」分四类**，只有 C、D 两类算真问题；A 类是已备案决策，B 类是排期问题。

---

## 0. 结论先行

- **数据面已基本抹平**：A1（数据拉满 + 锁屏组件）、A2（空气质量 + 智能摘要 + 掌控感）、A3（历史回看 + 设置收尾）三批**均已落地**，`git log` 可证（`f2c18eb` / `8b516bb` / `e525bed` / `48237d0` …）。
- 与 Android 本体的剩余差距**只剩三类**：专有源（已备案不做）、UI 氛围层（归方向 B）、以及 **A3 未做完的 3 个收尾项**。
- 另有 **5 处「代码写了但没接线」**——不是能力缺口，是实现缺陷，建议优先修。

| 分类 | 项数 | 性质 |
|---|---|---|
| A 有意不做（PRD-A §6 备案） | 7 | 决策，非遗漏 |
| B 归「方向 B」UI 氛围层 | 8 | 未排期 |
| C 计划内但未落地 | 6 | **真缺口** |
| D 已落代码未接线 | 5 | **真缺陷** |

---

## A. 有意不做（备案在 `PRD-zhisheng-ios-A.md` §6）

| 项 | 原版对应 | 不做理由（备案原文摘要） |
|---|---|---|
| 雷达图 | D24 | 中国天气网/彩云瓦片需凭据或地区限定；Open-Meteo 雷达仅欧洲。免密前提下无可用公开源 |
| 台风路径 | D25 | 浙江水利厅接口无文档无 SLA，断供即失效；且需地图渲染依赖，与 Core 零依赖纪律冲突 |
| 多数据源（和风/彩云/小米） | D27 | 和风=Ed25519 JWT、彩云=Token，均违背「免凭据即完整体验」；单源是特性不是缺陷 |
| 气象预警（国标四档着色） | D6 | 依附和风/彩云账号权限；Open-Meteo 无预警产品 |
| 应用内更新检查 | D35 | sideload 自用分发，无版本广播渠道；GitHub Releases 即入口 |
| 社区 / 赞赏 / 交流群 | README 相关 | fork 自用版本，无社区运营场景 |
| App Store 上架 / 账号 / 云同步 / 埋点 | D37 | 产品红线，永不做 |

> ⚠️ 注：原版「分钟级真 nowcast」在 §6 中记为 **「降级做」**（只做 Open-Meteo 15 分钟粒度简版降水卡），
> 但连降级版也**未落地**——故归入 C 类。

---

## B. 归「方向 B」（UI 打磨，未排期）

| 项 | 原版对应 | 说明 |
|---|---|---|
| 天气娘形象简报 | D34 | 规则引擎部分已并入 A2-2 摘要；缺的是形象/插画 |
| 天气氛围背景 12+ 种 + 强度可调 | D29 | 现为静态深色主题 |
| 横屏气象中枢 / 待机时钟 | D28 | 纯 UI |
| 发牌式卡组切换 | D15 | 重交互/动效 |
| 主题三档（深/浅/跟随） | D31 | 现固定深色 |
| 应用图标切换（天气娘↔经典） | D30 | 需设计资产 |
| 15 枚自绘天气图标 | D30 | 现用 SF Symbols 映射 |
| 街道级定位（精确到街道标签） | D18 | 反地理编码可做，PRD 定 P2 |

---

## C. 计划内、但确实未落地（真缺口）

| 项 | PRD 编号 | 代码证据 |
|---|---|---|
| 历史·往年同日（近 5 年并列） | A3-2 | `grep -E "往年|yearly|pastYears"` 无命中；仅 `ArchiveEndpoint` 支持单区间 |
| 历史·近 10 年同日 | A3-3 | 同上 |
| 多语言骨架（中/英） | A3-6 | `find -name "*.strings" -o -name "*.xcstrings"` **零文件**；`String(localized:)` 零调用 |
| 15 天页「天气娘简报」文案位 | A3-7 | 15 天页已落地（A2-9），文案插槽未见 |
| 短时降水卡（15 分钟粒度降级版） | §6「降级做」 | `grep minutely` **零命中**——连降级版都没做 |
| 遥测补充：能见度 / 露点 / 云量 / 阵风 | **未登记** | `grep -E "visibility|dewpoint|wind_gusts|cloud_cover"` 在 `Core/` 零命中。原版 README 明确有此项（「补齐可获取的能见度、露点、云量和阵风」），**PRD 缺口登记册本身漏登了这一条** |

---

## D. 已落代码、但未接线（真缺陷）

| # | 问题 | 证据 | 影响 |
|---|---|---|---|
| D-1 | **星标置顶未生效** | `CityListView.swift:42` 遍历 `viewModel.directory.cities`，而非 `directory.displayCities`；`displayCities` 生产代码零调用（仅测试） | 星标能点亮、能持久化，但城市**不会置顶** → 违反 A2-6 / AC-A2-18 |
| D-2 | **气压单位无设置项** | `SettingsView.swift` 无 `气压/hPa/pressure` 任何命中；原版为「温度、风速**和气压**单位分别设置」 | 气压格只能固定 hPa |
| D-3 | **设置页无应用内入口** | `SettingsView` 全仓仅 `ContentView.swift:60` 一处实例化，且位于深链路由注册内 | 只能长按图标快捷方式 / 深链进入，主界面无按钮 |
| D-4 | **异地城市时区未生效** | `City.timeZoneIdentifier` 已存储（`GeocodingMapper.swift:31`），但 `CityListView.swift:196` 只用作标签兜底；`timeFormatter` 未使用 | 查看外埠城市时，时间按**设备时区**渲染 |
| D-5 | **摘要预警位空转** | `WeatherSummaryEngine.alertSummary` 恒返回 `nil` | A2-2 规则优先级首位「预警位」永远不触发（A 类不做预警，此处属预期，但应在文档中显式承认为空实现） |

---

## E. 已对齐（无缺口，供回归核对）

实况温度/现象/体感 · 风向风速 · **气压（A1-1）** · 24h 逐时（A1-2） · 3/7/15 天逐日（A1-3）
日出日落（A1-4） · 昨日对比（A1-5） · 锁屏/灵动岛组件三族（A1-6） · 组件交互刷新（A1-7）
Home Screen 快捷方式（A1-8） · 空气质量六项 + 双 AQI（A2-1） · 一句话摘要（A2-2）
生活指数 4 项本地估算（A2-3） · 月出月落（A2-4） · 逐日行展开（A2-5） · 城市星标（A2-6，**置顶除外见 D-1**）
模块排序/隐藏（A2-7） · Large 逐时条（A2-8） · 15 天页（A2-9） · 历史近 7 日（A3-1）
设置页单位切换全端生效（A3-4） · 组件底色三档（A3-5） · 多城市收藏/排序/删除/搜索 · 无广告/无账号/无埋点

> 计数对照 `PRD-A §1.5` 基线：原 4 已对齐 / 9 部分 / 24 缺失 → 经 A1–A3 三批后
> **24 项缺失中 17 项落地**，剩余 7 项全部落在 A/B 两类（含 6 项已备案不做）。

---

## F. 建议动作（按性价比排序）

1. **D-1 星标置顶**（改 1 行：`cities` → `displayCities`）—— 一处改动即闭合一个已验收需求
2. **D-4 异地时区**（`timeFormatter` 接入 `City.timeZoneIdentifier`）—— 真机可见的显示错误
3. **D-3 设置页入口**（主屏工具栏加按钮）—— 目前设置页近乎「藏起来」
4. **C 类补齐**：先做 A3-2 往年同日（复用现成 `ArchiveEndpoint`，仅需年份循环 + 节流）；多语言可延后
5. **D-2 气压单位**：与 D-3 同批做，设置页一次补完
6. **补登**：把「遥测（能见度/露点/云量/阵风）」写入 PRD 缺口登记册，避免继续漏账
